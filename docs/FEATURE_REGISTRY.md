# Feature registry

What exists, what is being built, what is next, what is planned, what is research, and what must never
be built — so nobody builds a second copy or fakes a missing one. The long-term direction is
`docs/VELLUM_VISION.md`. A feature that moves to a later release changes here only — it never leaves the
Vision.

| Status | Meaning |
|---|---|
| **done** | shipped and tested |
| **done, unreleased** | implemented and tested on `main`, not yet in a release |
| **in progress** | being implemented in the approved current phase |
| **next** | approved for the current phase or the one after it, not started |
| **planned** | in the Vision, not started; no UI may pretend it exists |
| **research** | in the Vision, but needs an engine, method or owner decision before it can be planned |
| **out of scope** | excluded while Vellum is local-first; only the owner can change that |
| **do not build** | never |

## Now

- **Current release: 0.5.0** (tag `v0.5.0`, 2026-09-15). Object editing: text editing from 0.4, and in
  Edit mode selecting, moving, scaling, turning, mirroring, stretching, nudging and deleting text and
  pictures, one or several; alignment, distribution, snapping, picture replacement and insertion,
  paragraph grouping, overlap warnings and single-style paragraph reflow
  (record: `docs/planning/VELLUM_0.5.0_AUDIT.md`). Rows still marked *done, unreleased* below from before
  that release shipped in it.
- **Current cycle: 0.6.** One feature at a time, on the owner's instruction (2026-09-15). Done on `main`,
  not released: copy, paste and duplicate objects; cross-page and cross-document moves and paste; retyping,
  reflowing and replacing pasted copies; rotating text and free rotation; new text in a standard PDF font;
  new text boxes with several lines, wrapping and formatting; choosing the standard font family new text is
  written in; mixed formatting within one new text box; bundled fonts for new text, with the vendored font
  parser; new text in the document's own fonts; the bundled font library; formatting text the PDF already
  draws: size, colour, opacity and underline; bold and italic for text the PDF already draws, in the faces
  of its font the same PDF has (Editing). No version bump, push or release until the owner asks.

## Reading

| Feature | Status | Where |
|---|---|---|
| Open: dialog, drag and drop, double-click / "Open with", recent files, tabs | done | app.js, ui/tabs.js, ui/dropzone.js |
| Continuous and single-page layout, remembered per file | done | document-view.js |
| Zoom: presets, fit width/page, Ctrl+wheel and pinch, animated; fitted zooms re-fit as the window or sidebar resizes | done | document-view.js |
| View rotation (not saved) | done | document-view.js |
| Page thumbnails, document outline (bookmarks) | done | ui/sidebar.js, ui/thumbnails.js |
| Search with highlighting, match case, whole words | done | ui/findbar.js |
| Find and replace (V1.2): Ctrl+H (or "Replace text" in the command palette) shows a Replace row in the find bar; Replace (Enter) changes the highlighted match, Replace all (Ctrl+Alt+Enter) every match in the document as ONE undo step; plain text only, with match case and whole words. Replacing is retyping: each run is planned exactly as typing into it (planTextEdit — the run's own font, a standard font of the same style, or a refusal), so it saves, reopens and undoes like any text edit. The page's own text, pasted copies of text (planned as retyping the copy: the copied run's font, a standard font, or a refusal) and new text (its own font or a refusal), a match inside one line, and (V1.2) a match that runs on from one line of a paragraph to the next — a paragraph as grouping finds it (objects/text-block.js), so never across columns, a rule or unrelated text: highlighted by the find bar, and replaced by retyping both lines together (the replacement ends the first line, the rest of the second stays), in the same one undo step; a match across three or more lines, or one that would leave a line empty, is skipped with the reason; matches the engine can't change safely are left unchanged, and Replace all's notice says how many were replaced of how many and how many were skipped, with the reasons. Not: patterns, matches across more than two lines or outside one paragraph, pasted copies or new text across lines, other documents | done, unreleased | editing/find-replace.js, editing/session.js (replaceText, #addedLines, #lineGroups), ui/findbar.js, commands.js (find.replace); tests/editing/find-replace.test.mjs, tests/e2e/suites/find-replace.mjs |
| Password-protected PDFs (correct password only) | done | document-view.js |
| Damaged / missing / empty files explained | done | document-view.js |
| Page colours: normal, dark, sepia (display only) | done | css (page tone), themes |
| Print (with annotations) | done | print.js |

## Annotating

| Feature | Status | Where |
|---|---|---|
| Highlight, underline, sticky notes, freehand pen | done | annotations/ |
| Saved as standard PDF annotations, editable on reopen | done | annotations/persist.js |
| Encrypted PDFs: annotations kept alongside (sidecar) | done | MainWindow (annotations.*Sidecar) |
| Undo / redo (shared with page edits) | done | annotations/model.js |
| Strikethrough, shapes, stamps, text boxes, measure | planned | — |

## Editing

| Feature | Status | Where |
|---|---|---|
| Edit existing text, one line at a time: Edit tool (E), in place; Enter keeps, Esc cancels, Tab moves on | done | editing/, ui/text-editor.js |
| Written with the text's own font when it has every character; otherwise a matching standard font (said before applying) | done | editing/fonts.js, editing/edits.js |
| Only text pdf.js reads exactly the same way is offered; anything else says why (Type 3, symbol, vertical, forms, clipped, invisible, scans, soft masks, layers…) | done | editing/runs.js |
| Undo / redo shared with annotations and page edits; edits follow moved, duplicated and deleted pages | done | annotations/model.js, editing/edits.js |
| Saved into the page itself: only edited pages change; the replaced text is removed from the file | done | editing/page-writer.js, annotations/persist.js |
| Protected (encrypted) PDFs: text editing unavailable, with the reason | done | editing/session.js |
| Digitally signed PDFs: the first change is confirmed (saving invalidates the signature) | done | document-view.js, annotations/model.js, app.js |
| Tagged PDFs: says once that accessibility tags aren't updated for changed text | done | ui/text-editor.js |
| PDF/A: a change needing a substitute (non-embedded) font is refused, with the reason | done | editing/edits.js, editing/objects/text-run.js |
| Select an object in Edit mode: a text run or a picture, outlined where it is; identity only ({ page, key }) | done | editing/objects/selection.js, ui/text-editor.js |
| What may be done to an object, verb by verb, in the one reason vocabulary; a verb is true only where a writer exists | done | editing/objects/capabilities.js |
| Move and uniformly scale text and pictures: drag the object, or drag a corner handle | done | ui/text-editor.js, editing/objects/transform.js |
| Stretch a picture (non-proportional resize): drag an edge handle, along the picture's own width or height, from the opposite edge; never text, and one picture at a time | done, unreleased | editing/objects/transform.js (stretch), editing/objects/capabilities.js, ui/text-editor.js |
| Turn a picture a quarter turn ([ and ]) and mirror it in its own axes (Shift+H, Shift+V); since 0.6 text turns too (next rows) and freely from the rotate handle | done | ui/text-editor.js, editing/objects/image.js |
| Nudge with the arrow keys: one point, ten with Shift; a whole burst is one undo step | done | ui/text-editor.js, annotations/model.js |
| Delete a selected run or picture (Delete / Backspace); a picture's XObject is released when provably unused | done | editing/session.js, editing/objects/image.js |
| One object, one record, holding where it ends up: a second gesture replaces the first, and one gesture is one undo | done | editing/edits.js, editing/objects/image.js |
| Refused rather than risked: a clipped or degenerate picture, one inside a form, on a layer or under a soft mask; text is never mirrored, sheared or scaled non-uniformly (turning it is allowed since 0.6) | done | editing/objects/capabilities.js, editing/objects/image.js, editing/edits.js |
| Moved, scaled, turned and deleted objects — one or several — follow their pages through reorder, duplicate, rotate and delete, and through save and reopen; everything else in the file is kept (page boxes, annotations, links, form fields, outline, metadata, and images other pages still draw) | done | editing/edits.js (followEdits), annotations/persist.js; proved in tests/editing/object-pages.test.mjs and the page-changes suite |
| Line up and space evenly several selected objects (should-haves "alignment" and "distribution"): align left/right/top/bottom edges or centres, space evenly across or down, from an arrange bar over the selection or the command palette; edges as shown on screen whatever the rotation; one undo step, only when every object can be moved | done, unreleased | editing/objects/arrange.js, ui/text-editor.js, commands.js |
| Snapping while dragging (should-have): a dragged object or group snaps its edges or centre to another object's edges and centres, or the page's edges and centre, within 5 screen pixels, as the page is shown whatever the rotation; a thin guide line marks each line it is on while the hand is down; Alt held turns it off. Only the move changes (still a move, one record per object, one undo step) | done, unreleased | editing/objects/snap.js, ui/text-editor.js (#snappedMove) |
| Replace a picture (should-have "image replacement"): with one picture selected, "Replace picture…" on the bar over it or in the command palette opens a Windows file dialog; a PNG or JPEG takes the picture's place in exactly its frame (position, size, turn, mirror, shear, clip, drawing order), as the same object and one undo step. The image is embedded as a real XObject (a JPEG as it is, a PNG with its transparency as a soft mask); the old image resource is released only when nothing else on the page uses it. Refused with the reason: inline images, pictures that can't be moved, files that aren't a readable PNG or JPEG (or over 25 MB), PDF/A documents; tagged PDFs are told once that tags aren't updated | done, unreleased | editing/objects/image.js (readPicture, prepare, write), editing/session.js (replaceImage), ui/text-editor.js, MainWindow.xaml.cs (pictureDialog) |
| Insert a picture (should-have "image insertion"): in Edit mode, "Insert picture…" in the command palette or the page's context menu opens a Windows file dialog; a PNG or JPEG goes on that page as a new picture, centred and upright as the page is shown, at its natural size (96 pixels to the inch) but at most half the page, and is selected. It is an object like any picture: moved, resized, stretched, turned, mirrored, snapped, arranged, replaced and deleted, one undo step each, on blank and duplicated pages too, through save and reopen. Drawn after the page's own content under a new /XObject name, embedded once however many pages draw it; nothing already on the page changes. Refused with the reason: files that aren't a readable PNG or JPEG (or over 25 MB), pages whose content can't be rewritten, PDF/A documents; tagged PDFs are told once that the picture isn't added to the tags | done, unreleased | editing/objects/inserted-image.js, editing/objects/page-objects.js (insertedObject), editing/session.js (insertImage), ui/text-editor.js (insertPicture), app.js (context menu), MainWindow.xaml.cs (pictureDialog) |
| Paragraph grouping (should-have): in Edit mode, dragging a line that plainly belongs to a paragraph selects and moves the whole paragraph (then handles, keys, arrange and Delete act on it as on any selection, one undo step); a click still edits that one line, and a line selected on its own drags alone. Grouped only when every line can be moved, has the same font, size, colour, render mode and direction, starts at the same left edge at a spacing of 0.9–1.6 × the text size that doesn't drift, shares its row with no other text, doesn't start like a list item, and has nothing drawn between it and the next; an indented line leaves the lines around it ungrouped. Nothing new is written: each line is its own record, as before | done, unreleased | editing/objects/text-block.js, ui/text-editor.js (#paragraphOf) |
| Overlap warnings (should-have): while a drag or an arrow-key burst is moving objects, what they would newly cover (text or pictures, not what they already overlapped) is outlined in amber; the outline goes when the hand does, nothing is refused, and a drop that leaves an overlap is announced to screen readers | done, unreleased | editing/objects/overlap.js, ui/text-editor.js (#overlapsOf) |
| Several objects on one page: Shift- or Ctrl-click adds or removes one, a rectangle over bare paper selects what it wholly encloses (Shift or Ctrl adds), Ctrl+A selects the page's objects; a drag, the group's corner handles, the arrow keys, turn, mirror and Delete act on all of them as one undo step, and only when every one allows it (otherwise nothing changes, with the reason) | done, unreleased | editing/objects/selection.js, editing/objects/capabilities.js, editing/session.js, ui/text-editor.js |
| Single-style paragraph reflow (should-have, the narrow case): a whole paragraph selected in Edit mode that passes the reflow gate has one handle on its right edge; dragging it rewraps the paragraph's words, in order, into its own lines at that width — same baselines, left edge, font, size and colour, lines no longer needed emptied — one undo step, re-encoded only in the paragraph's own font (never a substitute). The handle is offered only when every line can be edited, is one style and one placement, on one left edge and even spacing, set left to right with the font's own widths (no kerning, tracking, justification or pen-move spaces) and with no hyphenated break. Refused with the reason when the hand lets go: a width narrower than a word, or one needing more lines than the paragraph has | done, unreleased | editing/objects/reflow.js, editing/session.js (reflowParagraph), ui/text-editor.js (#reflowable, #reflow) |
| Copy, paste and duplicate objects (0.6): in Edit mode, Ctrl+C copies the selected text and pictures as they are now (retyped, moved, replaced); Ctrl+V pastes them on the page in view, 10 pt right and down from where they were copied (a step further with each paste), selected, as one undo step; Ctrl+D duplicates the selection the same way; also in the command palette. A pasted copy is a new record (`text-copy`, `image-copy`) drawn after the page from the page's own glyphs, font, size, colour, ExtGStates and image resource, fingerprinted against the original content at every save, or another inserted picture for a picture from a file; copies move, scale, turn, delete, arrange and copy again, follow duplicated, reordered and rotated pages, and are ordinary content once saved. Refused with nothing stored: inline images; PDF/A for substitute fonts and new pictures. (Retyping, reflowing and replacing a pasted copy, and paste onto other pages and documents: the next rows.) | done, unreleased | editing/objects/copies.js, editing/session.js, ui/text-editor.js, commands.js; tests/editing/copy-paste.test.mjs, tests/e2e/suites/copy-paste.mjs |
| Cross-page moves and paste (0.6): selected text and pictures, one or several, pasted onto any page — or into another open document — land where they were on their page (or in the middle of a page they would miss altogether); Ctrl+X cuts (one undo step) so Ctrl+V on another page moves them; a move drag let go over another page puts them under the pointer there, as ONE undo step (session.moveObjectsToPage). A copy on another page keeps `from: { src, index }`, the page whose ORIGINAL content it draws; at every save the page writer reads that page before any page is rewritten (still in the plan, the opened file's own page, or a page of a PDF in the document's sources), checks the fingerprint, and adds the font, image XObject, ExtGStates and a named colour space or pattern to the destination page under fresh `VlCp…` names — the same objects, never re-encoded or rasterized. Survives save and reopen, undo/redo, and the origin page deleted, reordered or rotated and the destination duplicated. From another document, that document's PDF is kept once in this one's sources (as for inserted pages), with its already-verified page analysis. Refused with nothing stored or saved: a PDF/A destination for content from another document; an origin page missing, unreadable or changed; a resource the origin doesn't have; everything copy/paste already refuses | done, unreleased | editing/objects/copies.js (from, importer), editing/page-writer.js (readOrigins), editing/session.js (#pasted, moveObjectsToPage, #origin), ui/text-editor.js (cutSelected, #moveToPage), commands.js (edit.cutObjects); tests/editing/cross-page.test.mjs, tests/e2e/suites/copy-paste.mjs ("another page") |
| Editing pasted copies before saving (0.6): a pasted copy of text is retyped like its original — click it (or Enter with it selected) and type; the editor checks the text live — in the copied run's own font where it has every character, else a standard font of the same style, else refused with the reason; typed back to the original text it draws the file's own glyphs again, and emptied it goes, as deleting it does. A paragraph pasted together (lines pasted from one page; copies group into paragraphs only with other copies) is reflowed from its right-edge handle on the terms of any paragraph (objects/reflow.js), measured in the analysis of the page the lines were copied from. A pasted copy of a picture is replaced with a PNG or JPEG (Replace picture…) in its frame. Each is ONE undo step that changes the copy's one record — `text` and `encoding`, or `replacement` — keeping its id, fingerprint, `from` and placement, so the copy writer and the page writer draw it as before, on the same page, another page or from another document, through save and reopen; nothing is rasterized and no font is guessed. Refused with nothing stored: text neither the font nor a standard font can write, a substitute font in a PDF/A document, a new picture in a PDF/A document, pasted lines reflowed together with the page's own lines or with lines from another page, and everything reflow already refuses | done, unreleased | editing/session.js (#retypeCopy, #previewCopy, reflowParagraph, replaceImage), editing/objects/copies.js (copiedObject), ui/text-editor.js (copyItemOf, #paragraphOf, #reflowable); tests/editing/copy-edits.test.mjs, tests/e2e/suites/copy-paste.mjs |
| Rotating text and free rotation (0.6): selected text — one run, a paragraph, a pasted copy, or several objects with pictures among them — turns a quarter turn with [ and ] (each object about its own centre) or from the palette ("Turn objects left 90°" / "Turn objects right 90°"), and freely from the rotate handle standing off the selection's top edge (the whole selection about its centre; Shift keeps to 15° steps). Written as the move/scale transform already was — one absolute record per object, ONE undo step per gesture — and drawn by the existing text writer from the file's own glyphs under one `cm`: font, size, colour, colour space, ExtGStates/opacity and glyph codes untouched, nothing rasterized; saved and reopened it is ordinary editable text, turned. A stored text transform is always an exact similarity [p q −q p] (textPlacement), so repeated turns never add a skew. Turned text keeps its turn when retyped, moved, scaled, copied, pasted or cut to another page. Refused, with nothing stored: mirroring or stretching text (Shift+H/V stay pictures-only and are asked as a stretch), skews, and reflowing a turned paragraph (its width can't be measured along the page) | done, unreleased | editing/edits.js (textTransformRefusal, textPlacement), editing/objects/transform.js (similarityOf, rotateAbout), editing/objects/capabilities.js, editing/objects/copies.js (planCopy), ui/text-editor.js (#rotateHandle, rotateTo, turnSelected), commands.js; tests/editing/text-rotation.test.mjs, tests/e2e/suites/manipulation.mjs ("text turns") |
| New text (0.6, the first part of "new text boxes"): in Edit mode, "Add text" in the command palette or the page's context menu puts one line of text on that page — upright as the page is shown, centred from the palette and starting where the page was right-clicked from the context menu (kept on the page), Helvetica 12 pt, black — selected, with the editor open on it and its words selected, so typing replaces them; Enter keeps it. It is then retyped (checked live), moved, scaled, turned, snapped, arranged, copied, pasted (also onto other pages and documents), cut and deleted like any text, one undo step each; emptied it goes. A record of its own (`inserted-text`: text, font, size, box, transform — a similarity), drawn after the page's content as real text (`BT … Tj ET`) in a standard PDF font under a new /Font name, encoded in that font's standard encoding; nothing on the page is touched, nothing rasterized; saved and reopened it is ordinary editable page text. Refused with the reason, nothing stored: characters the standard font doesn't have (never drawn in another font), PDF/A documents (the standard fonts aren't embedded), pages whose content can't be rewritten, mirrors and skews; a line of new text is never grouped into a paragraph or reflowed. Tagged PDFs are told once that new text isn't added to the tags. Several lines, wrapping and formatting: the next row | done, unreleased | editing/objects/inserted-text.js, editing/objects/page-objects.js (insertedTextObject), editing/objects/registry.js, editing/objects/copies.js (snapshotOf), editing/session.js (insertText, #retypeNewText, #previewNewText), ui/text-editor.js (addText), commands.js (edit.addText), app.js (context menu); tests/editing/new-text.test.mjs, tests/e2e/suites/manipulation.mjs ("new text"), tests/e2e/suites/new-text-ux.mjs |
| New text boxes: several lines, wrapping and formatting (0.6): new text is a box of lines — Shift+Enter in its editor starts a new line (Enter still keeps it), and the right-edge handle of one selected box sets the width its lines wrap to (at spaces; a word wider than the box breaks between characters), measured along its baseline however it is turned. Selected new text (one box or several, all new text) shows a format bar in the selection bar, and the palette has the same actions: Smaller/Larger text (steps 6–144 pt), Bold, Italic (the standard family's own faces: Helvetica Bold/Oblique, never a synthesized style), Underline, Align left/centre/right, Text colour (six swatches or a custom colour) and Opacity (100/75/50/25%); each change is one undo step for every selected box and keeps its top-left corner in place. Record `inserted-text` gains underline, align, color (#rrggbb), opacity and width (null: as typed); a record holding only font and size reads as the defaults. Layout is font-agnostic (editing/objects/text-format.js layoutText over a "face": ascent, descent, underline metrics, advance, missing, codes), with standardFace() from pdf-lib's metrics, so a parsed font can supply another face later. Drawn as before, in one `BT`: one `Tm … Tj` per line (lines 1.2 × size apart), `rg`/`g` fill, an ExtGState of its own (`/ca /CA`) only when not opaque, and each underline a filled rule after `ET`; moving, turning, copying, pasting onto other pages and documents, cutting, undo and save keep the format. Refused with the reason, nothing changed: a size, width, colour, opacity or alignment outside its range; a character the chosen face doesn't have; formatting anything that isn't new text; PDF/A as before. Limits: line spacing is fixed and there is no justified alignment; the editor shows browser stand-ins for the standard fonts, so a line may wrap slightly differently while typing than on the page; saved and reopened, the lines are ordinary separate lines of page text and the underline a separate rule, no longer one formatted box; no keyboard shortcuts for formatting (palette and bar only) | done, unreleased | editing/objects/text-format.js (new), editing/objects/inserted-text.js (planNewText, planFormat, write), editing/objects/text-run.js (addResource), editing/objects/page-objects.js (format), editing/session.js (formatText), ui/text-editor.js (textarea editor, #layoutLines, format bar, formatSelected, wrap handle), commands.js (edit.text*), icons.js, css/app.css; tests/editing/new-text-format.test.mjs, tests/editing/new-text.test.mjs, tests/e2e/suites/manipulation.mjs ("new text") |
| Font selection for new text: the standard families (0.6): selected new text — one box or several — is written in another standard family, from the format bar's font menu (Helvetica, Times, Courier, each name shown in the browser font that stands in for it) or the palette's "New text in …". The family's own bold and italic are kept (Helvetica-Bold → Times-Bold, never a synthesized slant), the box's lines are laid out and wrapped again in the new font's own widths, its top-left corner stays where it is, and it is ONE undo step for every selected box, keeping each record (`inserted-text`; only its `font` changes) with its id, placement and the rest of its format. The bar's size also opens a menu of the sizes 6–144 pt, beside Smaller and Larger. Saved and reopened, the lines are ordinary editable page text in that standard font. Refused with the reason, nothing changed: a font Vellum doesn't have (bundled fonts came later, in their own row below; the document's own fonts aren't offered yet); a character the chosen face can't write; page text (changing the font of text the file already draws is not this); PDF/A as before | done, unreleased | editing/objects/text-format.js (FAMILIES, FAMILY_NAMES, styledFont `family`), editing/objects/inserted-text.js (planFormat), editing/session.js (formatText), ui/text-editor.js (#fontMenu, #sizeMenu, format bar), ui/menu.js (an item's font), commands.js (edit.textFont*); tests/editing/new-text-format.test.mjs, tests/e2e/suites/manipulation.mjs ("new text") |
| Mixed formatting within one new text box (0.6): the format controls sit on the open editor's own bar too, and there act on the characters selected in the editor — font family, bold, italic, size, underline, colour and opacity of those characters alone; alignment and the wrapping width stay the whole box's. With nothing selected they act on all of it; over a selected (not open) box they act on the whole box, as before. Text typed but not yet kept goes into the same step, so typing and formatting a word is ONE record and ONE undo step, and the editor stays open with the selection where it was. The record `inserted-text` gains `spans` ([{ n, font?, size?, underline?, color?, opacity? }] over the text, only the fields that differ from the box's own), kept in a normal form: adjacent spans that read alike are joined, each box field is what most characters read in, and a box that reads alike all through has no `spans` — so records from before need no conversion. Retyping carries the formatting of what stayed; typed characters take the format of the character before them. Layout lays out and wraps a line piece by piece, each piece measured in its own face and size, a line spaced by its largest size; the writer draws one `Tm` per line and one `Tj` per piece, setting `Tf`, fill and ExtGState only where they change, and underlines piece by piece. Each character is checked against its own face. The editor shows the pieces in their formats behind the typed text. Moving, turning, copying, pasting, undo and save keep the spans; saved and reopened, each piece is ordinary editable page text in its own font. Refused with the reason, nothing changed: a character the chosen face doesn't have, anything already refused for new text; formatting part of a box across a selection of several boxes. Limits: the typing cursor is placed by the box's own size, so in a line of mixed sizes it can sit slightly off the glyphs; only the standard families (no document or bundled fonts); no per-span alignment or line spacing | done, unreleased | editing/objects/text-format.js (formatRuns, normalizeRuns, applyChanges, remapSpans, standardFaces, layoutText pieces), editing/objects/inserted-text.js (planNewText, planFormat range/text, write), editing/objects/page-objects.js (spans), editing/session.js (formatText range/text, #retypedNewText), ui/text-editor.js (editor format bar, #editorRuns, #paintMixed, #refreshEditor), css/app.css; tests/editing/new-text-spans.test.mjs, tests/e2e/suites/manipulation.mjs ("new text") |
| Font selection for new text: bundled fonts (0.6): the fonts new text may be written in are one font set (editing/objects/font-set.js) — the standard families, then the families bundled with Vellum (web/fonts/document, listed in editing/objects/bundled-fonts.js with their licence beside them) — and the format bar's font menu lists every family of the set the same way, each shown in its own face (a bundled face is given to the browser from its own file; the standard ones keep their stand-ins). A record's `font` is a font key: a standard font's PostScript name as before, or `bundled:<family>/<style>` (regular, bold, italic, bold-italic); spans take either, so one box mixes them. A bundled font is read by the vendored fontkit (web/vendor/fontkit, @pdf-lib/fontkit 1.1.1, MIT; with pako 2.1.0) the first time it is used, measured as pdf-lib writes it — glyph advances with nothing shaped between glyphs (no ligatures or contextual alternates), so a piece measures the same wherever a line breaks — and embedded when saved, once per document (inserted-text.js prepare), as a subset of the glyphs its text shows (Identity-H, with ToUnicode); saved and reopened, each piece is ordinary editable page text in that embedded font. A family lists only the faces it has; bold and italic use them and are refused where it hasn't. Refused with the reason, nothing changed: a family whose files can't be read (never offered), a face the family hasn't got, a character the face doesn't have, a font whose licence flags (OS/2 fsType: restricted, preview & print, no subsetting, bitmap only) don't allow it to be embedded in an editable PDF; PDF/A as before (an embedded subset isn't checked against the standard). The command palette has a “New text in …” command for each bundled family too (commands.js), beside the standard ones. The fonts that ship: the bundled font library (the row below the document's own fonts); the mechanism itself is tested with test registrations of the Liberation Sans files pdf.js ships. Also refused: characters of scripts that need shaping or run right to left (Hebrew, Arabic, the Indic scripts, Thai and the like, combining marks), which glyph by glyph would come out wrong. Limits: no shaping | done, unreleased | editing/objects/font-set.js (new: registerBundledFamily, fontSet, fontkitFace, embedBundledFonts), editing/objects/text-format.js (font keys, STANDARD_FAMILIES, bundledKey, styleOf/styledFont/applyChanges over families, layout measured per drawn piece), editing/objects/inserted-text.js (fonts, prepare, write through faces), editing/objects/page-objects.js, editing/session.js (#fonts, fontFamilies), ui/text-editor.js (#fontMenu from the session, cssFont), vendor/fontkit, vendor/pako; tests/editing/new-text-fonts.test.mjs |
| Font selection for new text: the document's own fonts (0.6): the font menu also lists the fonts the opened PDF already has — after the standard families, before the bundled ones, grouped into families by name with each font's own bold and italic, each shown as pdf.js loaded it for the page — and choosing one writes the selected new text (a box, several, or characters of an open editor) in that font object of the file. A font is offered once pdf.js has confirmed glyphs of it on a page read in Edit mode, and only when it is embedded, drawn left to right, not symbolic and not Type 3 (simple TrueType/Type 1/CFF and composite Identity or embedded-CMap fonts). Only characters the document has been seen to draw in that font are written, each with exactly the code and width pdf.js drew it with (editing/fonts.js planText); a space the font doesn't draw is a gap as wide as its space (or a quarter em) in a TJ. The font program is never changed or read, and the file gains no font. Record `inserted-text` fonts may be `doc:<object>-<generation>`, and the record gains `glyphs` ({ key: { character: [code, byteLength, width] } }) — the glyphs it is written with; the writer checks each against the font dictionary it adds to the page (the code is one character in the font, means that character, has that width) and refuses the save when any doesn't match. Where a family has two fonts of one style (two subsets), the one with more confirmed glyphs is used. Saved and reopened, the text is ordinary editable page text in that font. Refused with the reason, nothing changed: a character the document hasn't been seen to draw in that font ("Vellum can only write the characters it has seen the document draw in it"), a style the family hasn't got, pasting such text into another document (the font object means nothing there); PDF/A as before. Limits: fonts of pages inserted from another PDF aren't offered; the underline position of a document font is Vellum's own (a tenth of an em below the baseline); the document's own fonts aren't in the command palette | done, unreleased | editing/objects/font-set.js (documentFontSet, withDocumentFonts, tableFace, checkedDocumentFaces), editing/objects/text-format.js (documentKey), editing/objects/inserted-text.js (glyphs, write with `source`), editing/objects/page-objects.js (families), editing/session.js (#fonts with the base source's fonts and pdf.js's loaded names, paste refusal), ui/text-editor.js (#fontMenu read each time, noteFamilies, cssFont); tests/editing/new-text-document-fonts.test.mjs |
| Bundled font library (0.6): 38 families ship in web/fonts/document, each an unchanged static TrueType font as Google Fonts distributes it, under its own name (Liu San: see its row), with its SIL Open Font License 1.1 (OFL.txt) beside it and a line in NOTICE.txt (copyright, project). Sans: Inter, Plus Jakarta Sans, DM Sans, Figtree, Urbanist, Montserrat, Poppins, Rubik, Nunito Sans, Work Sans, Public Sans, IBM Plex Sans, Source Sans 3, Fira Sans, Barlow, Liu. Serif: Source Serif 4, IBM Plex Serif, Libre Baskerville, Merriweather, Lora, Literata, Newsreader, Cormorant Garamond, Crimson Pro, DM Serif Display (regular and italic only: the family has no bold), Playfair Display, Bitter. Mono: JetBrains Mono, Fira Code (regular and bold), IBM Plex Mono, Source Code Pro, Space Mono. Legible: Atkinson Hyperlegible, Atkinson Hyperlegible Next, Atkinson Hyperlegible Mono. International: Noto Sans, Noto Serif (Latin, Greek, Cyrillic, Vietnamese). Each family lists regular, bold (700), italic and bold italic where it has them. The catalog is static data (editing/objects/bundled-fonts.js: id, name, group, faces); the font set lists every family without reading a file and reads a face when it is wanted (fontSet `load`: the faces the document's new text uses, a family being chosen, text being pasted), so opening Edit mode reads no font; a family any face of which can't be read is no longer offered. The font menu separates its groups (standard, the document's, then sans, serif, mono, legible, international) and scrolls. About ~24 MB of fonts (~6.5 MB compressed). Not bundled: Manrope, Outfit, Sora, Space Grotesk, Inconsolata (regular and bold only, covered by families with italics); Zilla Slab (its bold italic's licence flags say preview & print only); Ubuntu, Ubuntu Mono (Ubuntu Font Licence, not the OFL: left for the owner); Iosevka, Monaspace (not distributed as a few static TrueType faces; Monaspace's texture healing is contextual alternates Vellum doesn't shape); script-specific Noto and CJK families (the scripts needing shaping are refused anyway; CJK faces are several MB each). Liu is added (its own row) | done, unreleased | editing/objects/bundled-fonts.js (new), editing/objects/font-set.js (catalog registration, groups, lazy fontSet load), editing/session.js (#fonts wanted faces), ui/text-editor.js (#fontMenu groups), css/app.css (.font-menu), web/fonts/document; tests/editing/bundled-fonts.test.mjs, tests/editing/new-text-fonts.test.mjs, tests/e2e/suites/manipulation.mjs ("new text") |
| Formatting text the PDF already draws (0.6): selected page text — one run, several, or pasted copies of runs — is made larger or smaller, underlined, coloured and made more or less opaque from the command palette (Larger text, Smaller text, Underline text, Text colour…, Text opacity…; the colour and opacity menus open beside the selection), ONE undo step for all of it; no floating bar stands over page text (it would cover the rotate handle). Nothing is re-encoded: the run is redrawn by the existing text writer from its own glyph codes in its own font object, as for a move, with only this changed — a size is a uniform scale about the run's first glyph origin composed into the record's `transform` (so the glyphs, Tc/Tw/Tz spacing, rise and TJ adjustments all scale together and the baseline start stays put), and the record (`text` or `text-copy`) gains `format` { color?, opacity?, underline? }: a DeviceRGB fill in place of the original's, an ExtGState of its own (/ca /CA) set after the original's ExtGStates (blend mode and the rest kept), and an underline as a filled rule in the run's own text space, a tenth of an em below the baseline and a twentieth thick, as wide as the glyphs drawn (measured with the PDF text model over the widths pdf.js confirmed, or the standard font's widths for retyped text in a substitute font). Each field is stored only when it differs from the page's own, and a run's record that no longer says anything goes. Retyping, moving, turning, copying, pasting (also to other pages and documents), undo and save keep the format; saved and reopened it is the same editable text, in the same font, at the new size, colour and opacity, with the underline a separate rule. The writer checks the format again before any byte is written. Refused with the reason, nothing changed: a font, bold or italic (another face would re-encode text in a font it isn't drawn in — never substituted); alignment or a width (a line of the page isn't a box); part of a line; colour or underline of text drawn as an outline (render modes 1 and 2); colour or opacity in a PDF/A document; sizes outside 1–1000 pt; new text and page text in one step; reflowing a paragraph with a formatted line; and everything already refused for editing a run (runs.js). Limits: the colour shown for text in a colour space other than device grey or RGB is unknown, not converted; the underline position is Vellum's own, not the font's | done, unreleased | editing/objects/run-format.js (new: planRunFormat, runFormatOf, runFormatRefusal), editing/objects/text-run.js (drawText style, styleOf, opacityStates, refuseFormatsInPdfa), editing/objects/copies.js (format on copies), editing/edits.js (format carried), editing/session.js (formatText → #formatRuns), editing/objects/reflow.js (formatted refusal), ui/text-editor.js (#pageTextFormats, textColourMenu, textOpacityMenu), commands.js (edit.textColour, edit.textOpacity); tests/editing/existing-text-format.test.mjs, tests/e2e/suites/manipulation.mjs ("page text format") |
| Bold and italic for text the PDF already draws (0.6): a selected line of page text — one run, several, or pasted copies of runs — is set in another face of its own font family that the same PDF already has (regular ↔ bold, regular ↔ italic, bold ↔ bold italic, and any other of the four the file has), from the command palette (Bold text, Italic text), ONE undo step for all of it. The face is the one the document's own font set lists for that family and style (the same family name; bold and italic from the font itself): an embedded font object of the opened file, confirmed by pdf.js. Nothing is guessed or re-encoded and the font program is never read or changed: the run is redrawn by the existing text writer exactly as for a move (its first glyph's placement, size, spacing, colour, ExtGStates, the gaps between its glyphs, the record's transform), only in the sibling font object, each glyph's character drawn with the code and width pdf.js confirmed that font draws it with; a space that font hasn't been seen to draw is a gap as wide as its space, with the line's own character and word spacing. The record (`text` or `text-copy`, encoding `original`) gains `face` { font: `doc:<object>-<generation>`, bold, italic, glyphs }; back in the run's own style it goes. The writer checks the table against the font object (as for new text in the document's fonts) and that every glyph of the run is in it before any byte is written. The line keeps its baseline start and grows or shrinks along its baseline; nothing else moves. Size, colour, opacity and underline, moving, turning, copying and pasting (in the same document), undo and save keep the face; saved and reopened it is the same editable text in that font, the original not drawn as well, no font added. Refused with the reason, nothing changed: a face the PDF hasn't got (or hasn't been seen to draw, or of a font that isn't embedded); a face that hasn't been seen drawing every character of the line; a glyph standing for several characters; a retyped line; retyping a line set in another face (set it back first); a line whose new width would newly cover another object on the page or another changed line (the overlap test of gestures, a tenth of the line's height counting), or reach further past the page's box or its clipping shape; text from a page inserted from another PDF, and pasting it into another document; reflowing a paragraph with such a line; another font family. Limits: part of a line; faces the file has but pdf.js hasn't confirmed on a page read in Edit mode | done, unreleased | editing/objects/run-face.js (new: planRunFace, runStyleOf, faceGlyphsOf, faceQuadOf, drawnQuadOf, runFaceRefusal), editing/objects/text-run.js (documentFaces), editing/objects/copies.js (face on copies), editing/objects/font-set.js (documentFamilyOf), editing/objects/run-format.js (bold/italic in runFormatOf), editing/edits.js (face carried), editing/session.js (#formatRuns faces, #refuseFaceGeometry, retype and paste refusals), editing/objects/reflow.js (face refusal), ui/text-editor.js (#liveOf face geometry), commands.js (Bold text, Italic text); tests/editing/existing-text-face.test.mjs, tests/e2e/suites/manipulation.mjs ("page text face") |
| Font family for text the PDF already draws (0.6): the edit bar of a line of page text (a run or a pasted copy of one) has a Font button — "Font: <family>" — whose menu lists the families of the opened PDF's own fonts, the line's own checked, each shown in itself. Choosing one sets the whole line in that family's face of the style the line reads in (bold and italic kept), from the same planner, writer and checks as bold and italic (objects/run-face.js): the face must be an embedded font object of the same file with a glyph pdf.js confirmed for every character of the line, the record's `face` holds it, the writer checks the table against the font object, and the new width must not newly cover another object or leave the page or clip — nothing else moves. Text, baseline start, size, colour, opacity, underline, rotation and transform are kept; one undo step; saved and reopened it is editable page text in that font object; its own family again, the face goes. Also `formatSelected({ family })` on selected page text. Every embedded text font family the PDF names is listed (editing/source.js scanFonts: every page's font resources and those of its forms, no font program read), also one on a page not read yet or never seen drawing, once per family: a family the line can't be set in is listed, marked "(can't be used for this line)", but can't be chosen. Refused with the reason, nothing changed: standard and bundled families (never used for text the PDF already draws), a family the PDF hasn't got, a style it hasn't got, characters it hasn't drawn, ligatures, retyped lines, width, text from another PDF. Limits: bundled-font substitution for page text is not done; changing the font of several lines at once is only through formatSelected | done, unreleased | editing/objects/run-face.js (runFamilyOf, planRunFace family), editing/session.js (formatText family, runFontFamilies), editing/source.js (scanFonts), editing/objects/font-set.js (embeddedFamilies), ui/text-editor.js (#runFontMenu, #syncRunFont, #setRunFont), css/app.css; tests/editing/existing-text-family.test.mjs, tests/editing/fixtures.mjs ("families"), tests/e2e/suites/page-text-font.mjs |
| General paragraph reflow (more lines than a paragraph has, mixed styles, kerned or justified text, hyphenation, turned paragraphs), new characters outside Latin (WinAnsi) | planned | — |

## Pages

| Feature | Status | Where |
|---|---|---|
| Rotate, delete, duplicate, insert blank, drag to reorder | done | pages/, ui/thumbnails.js |
| Organize pages (0.8): in the page thumbnails, Ctrl+C copies the selected pages (or the current one) and Ctrl+V pastes copies after the last of them (also in the thumbnail context menu and the command palette: Copy pages, Paste pages after); Ctrl+D duplicates; Alt+Up and Alt+Down move the selected pages one place, together. Each is one undo step; the copies carry their annotations and object edits (pages/plan.js copyEntries, the same followPages and followEdits as duplicate). Pasting goes into the document the pages were copied from only (another PDF: Insert pages from file). The PDF's own form fields follow the page plan when saved: a repeat of a page (duplicate, paste) shows the same fields — its widgets become more widgets of them, sharing one value, a field that is its own widget split into a field with kids — never copies outside the form; the widgets of deleted pages leave their fields, a field left with none leaves the form, and its kept value is dropped rather than refused. Limits: form fields of pages inserted from another PDF aren't added to the form | done, unreleased | pages/plan.js (copyEntries), pages/actions.js (copy, paste, moveBy), document-view.js (copyPages), commands.js (pages.copy, pages.paste, pages.moveUp, pages.moveDown, Ctrl+D), annotations/persist.js (followFormFields); tests/editing/forms.test.mjs, tests/editing/object-pages.test.mjs, tests/e2e/suites/page-changes.mjs ("organiser keys") |
| Insert pages from another PDF (menu or drop) — i.e. merge | done | pages/actions.js |
| Extract pages to a new PDF, split into files | done | pages/actions.js |
| Deleted pages really removed on save; bookmarks kept | done | annotations/persist.js |

## Forms

| Feature | Status | Where |
|---|---|---|
| Filling the PDF's own form fields (0.7, Forms v1): the AcroForm fields pdf.js finds are drawn as inputs over the page — text fields, checkboxes, radio buttons, dropdowns and list boxes — and what is typed or chosen is kept by field name (not an undo step; the field is where it's changed back) and marks the document changed. Saved into the same fields: /V, a checkbox or radio button's /AS, a choice field's export value, and a new appearance in Helvetica so other readers show it; where Helvetica can't show the value (or a choice's export value isn't the text it shows) /NeedAppearances is set instead. The fields stay real, interactive fields: never created, removed, flattened or rasterized; no form JavaScript or actions run. Page changes rebuild with the filled values. Refused with the reason: a field no longer in the file. Protected (encrypted) PDFs can be typed into but not saved (a notice says so). Limits: filling is not in undo; appearances use Helvetica, not the field's own font; no XFA forms | done, unreleased | forms/fields.js (readFields, valueOfInput, writeFormValues), annotations/model.js (setFormValue, formValues), annotations/persist.js (composeDocument forms), document-view.js; tests/editing/forms.test.mjs, tests/e2e/suites/forms.mjs |
| Creating form fields (Forms v2): "Add form text field", "Add checkbox", "Add radio button" and "Add dropdown" (command palette, then click the page; or the page's context menu, in its own "Form Fields" section — "Add Text Field here", with its own icon, apart from Edit mode's "Add Text Box" under "Page Content"; a field right-clicked is headed "Form Field · Text Field" (or its kind), a selected text box "Text Box") place a new field there, named Text1, Check1, Choice1 (radio buttons placed one after another join one group, values Option1, Option2…), Dropdown1 (options Option 1, Option 2), and select it. Until saved it is an item of the edit store (type `field`: kind, rect, name, value, options), drawn by the annotation layer: dragged to move, resized from its corner handle, deleted with Del — each one undo step, following its page through page changes. Its bar sets the name (a radio button's group and value) and a dropdown's options, refusing an empty name, a dot, or a name another field uses. Saved as ordinary AcroForm fields with widgets and appearances (pdf-lib), not Vellum annotations: reopened, they are filled like any other field (Forms v1). Refused with the reason: a name already a field of the file; protected (encrypted) PDFs. Limits: after reopening they are the file's own fields, edited as Forms v2.1 below; no filling before saving; no fonts or tab order; no signature fields, list boxes, buttons, JavaScript or actions | done, unreleased | forms/fields.js (FIELD_KINDS, uniqueFieldName, writeNewFields), annotations/persist.js (composeDocument), annotations/layer.js (startField, addFieldAt, #fieldShape, #startFieldDrag, #showFieldBar), annotations/geometry.js, document-view.js (fieldNames, notify), commands.js (forms.add*), app.js (context menu), ui/menu.js (headings), css/app.css; tests/editing/forms-create.test.mjs, tests/e2e/suites/forms.mjs |
| Editing the file's own form fields (Forms v2.1): right-click a text field, checkbox, radio button, dropdown or list box of the file → "Edit Form Field" makes it an item of the same edit store as a created field (type `field`, with `existing`: the field's name and the widget's original rectangle), drawn by the annotation layer in place of pdf.js's own widget: dragged to move, resized from its corner handle, deleted with Del (kept as `deleted` so it undoes) — each one undo step, and picking it is one too. Its bar (created fields too) sets the name, Required, Read-only and a text field's maximum length; a radio button's group is renamed, made required or read-only as one; an existing radio button's export value and an existing dropdown's or list box's options are shown or kept, never changed. Saved into the same AcroForm field (values first, under the name they were typed under): the widget's /Rect (a resized text or choice field is redrawn in Helvetica, checkboxes and radio buttons keep their appearance, scaled), /T, the Required and ReadOnly flags, /MaxLen; a deleted field is removed with its widgets, a deleted radio button only its widget and /Opt entry. Refused with the reason: an empty name, a dot, a name another field uses (in the bar, and again when saving), renaming a field inside another ("parent.child"), a maximum length shorter than the text already there, radio buttons of one group given different names; protected (encrypted) PDFs. Limits: no option or export-value editing for existing fields; no JavaScript or actions, tab order, fonts, signature fields, push buttons or new field types; fields whose page was deleted are skipped | done, unreleased | forms/fields.js (existingFieldItem, validFieldName, writeFieldChanges), annotations/persist.js (composeDocument), annotations/layer.js (existingFieldAt, editExistingField, #hideEditedFields, #showFieldBar), annotations/geometry.js (hitTest), app.js (context menu), css/app.css; tests/editing/forms-edit.test.mjs, tests/e2e/suites/forms.mjs |
| Imported form fields: pages inserted from another PDF (or pasted, duplicated) keep their AcroForm fields — text fields, checkboxes, radio groups, dropdowns, list boxes — as fields of the file's own form, with their values, geometry, Required / Read-only flags and appearances; the other PDF's default appearance and form fonts come too. Only widgets on the pages brought in come (a radio group keeps the buttons and export values on those pages); a repeat of an inserted page shares its fields and value, like a repeat of the file's own page; deleting an inserted page takes its fields out. A name already in the file is never overwritten: the imported field becomes name_2, name_3… (every inserted PDF counts, in the order it was added, so names don't shift when other pages are deleted); values typed before saving are kept under those names, and dropped when their page is deleted. The other PDF is only read. Refused as before: protected (encrypted) PDFs. Limits: signature fields and push buttons come as plain annotations, not fields; no JavaScript or actions added, none removed | done, unreleased | annotations/persist.js (adoptImportedFields, importedFieldNames, followFormFields, composeDocument); tests/editing/forms-import.test.mjs, tests/e2e/suites/forms-import.mjs |
| Redaction v1, text and pictures: in Edit mode, "Redact selection" (command palette) marks the area each selected object covers, one undo step. When the document is composed, redaction runs last on the page, over every other edit: each glyph whose box touches an area is taken out of its text operator (its advance kept, so the rest of the line doesn't move), visible or invisible; each picture whose box touches an area has its draw removed — the whole picture — and its resource dropped when nothing else on the page draws it, so the saved file's garbage collection removes its data and the page's old content stream; the page's /Thumb is dropped; the areas are painted black. Redacted objects can't be selected. Refused with the reason, nothing saved: a form XObject reaching an area, text whose position isn't known exactly, ActualText or tagged content in an area, text used as a clip, a link or annotation over an area, a vector shape (fill or stroke, with its line width) or shading reaching an area — shapes can't be removed yet and are never left under the black box. Forms, shapes and shadings are refused when "Redact selection" runs; the other refusals show when the pages update. Limits: metadata, bookmarks and other copies of the information elsewhere aren't touched | done, unreleased | editing/objects/redaction.js, editing/page-writer.js (rewritePage), editing/objects/text-run.js (neutralize), editing/session.js (redactAreas), ui/text-editor.js (redactSelected), commands.js; tests/editing/redaction.test.mjs, tests/e2e/suites/redaction.mjs |
| Redaction v2, from Find's own matches: "Redact all" beside Replace all in the find bar (Ctrl+H) counts, then redacts, every verified occurrence of the current search across the whole document as ONE undo step — the same records and the same true-redaction engine as "Redact selection" (objects/redaction.js), never a second one. A match is found the way Replace all finds one (find-replace.js findMatches, per run, never across a run or a line), but only in a page's own text that this session hasn't already retyped: its area is the exact box of the matched glyphs' own quads (never a guessed box), so a match beside other text on the same line never takes it too. Before anything changes, a dialog shows how many occurrences will be redacted; matches this can't answer for — already-edited text, or a run whose position isn't known exactly (the same checks "Redact selection" and composing rely on) — are left alone and counted separately with the reason, never silently included or weakened. Not: matches across two runs or lines, pasted copies or new text, matches in already-edited text (redact those with "Redact selection" instead) | done, unreleased | editing/runs.js (groupRuns run.chars), editing/session.js (findRedactableMatches, applyMatchRedactions), ui/findbar.js (redactAll), commands.js (find.redactAll); tests/editing/redact-matches.test.mjs |
| Fill & Sign v1, signatures: in Edit mode, "Add signature…" in the command palette or the page's context menu opens a dialog to type a name (in a Windows handwriting font), draw with the mouse or pen, or import a PNG or JPEG; black or blue ink. A typed or drawn signature is trimmed to its ink and kept as a transparent PNG at three times screen density; it goes on the page as an inserted picture (centred, upright, signature-sized), so it is moved, resized, turned, deleted and undone like any picture, and saved as real page content (an image XObject with a soft mask), not a flattened page. Not a certificate-based digital signature | done, unreleased | ui/signature.js, ui/text-editor.js (addSignature), editing/objects/inserted-image.js (defaultPlacement pointsPerPixel), commands.js, app.js (context menu), MainWindow.xaml.cs (pictureDialog title); tests/editing/signature.test.mjs, tests/e2e/suites/signature.mjs |

## App

| Feature | Status | Where |
|---|---|---|
| Custom title bar, single instance, file association, jump list | done | C# host |
| In-app updates from GitHub releases (verified by SHA-256); Vellum's only network feature, daily check can be switched off (WebView2's own traffic: WEBVIEW2_NETWORK_AUDIT.md). "Update now" shows Downloading, Verifying, Installing and Restarting in Vellum's own dialog; Setup runs with no window (/VERYSILENT) once Vellum has closed, then Vellum restarts by itself and reopens the documents. If Setup fails it rolls back and starts the version that was installed, which says the update didn't install | done (no-window install: unreleased) | Services/Updater.cs, MainWindow.Updates.cs, ui/updates.js, installer/InAppUpdate.iss; tests/e2e/suites/updates.mjs |
| Colour themes (7) in Light / Obsidian dark / System, custom accent | done | themes.js, app.css |
| Settings window (Appearance, Reading, Updates, Shortcuts, About) | done | ui/settings.js |
| Command palette (Ctrl+K): all commands + recent files | done | ui/palette.js |
| Home screen: greeting, Open card, recent documents with first-page covers | done | ui/start.js, recent-covers.js, RecentFiles.cs |
| Document collections V1: named lists of documents, shown under the recent ones on the home screen. New collection, Rename, Delete; "Add PDFs…" picks files in the Open dialog, and a document already in the recent list or open in Vellum can be added from the page. A card opens the document the way a recent one does (the host opens only paths it already knows: the recent list or a collection). A collection holds full file paths and nothing else — no PDF is copied, moved or changed, a file is never listed twice (same path, any case), two collections can't share a name, a file that is gone is shown as missing until it is removed, and removing a document or deleting a collection leaves every PDF where it is. Kept in %LOCALAPPDATA%\Vellum\collections.json (a damaged file is kept aside as .bad and starts empty), separate from the recent list, document history and anything a document's editing session holds. Limits: no tags, notes, ordering, nesting, search within a collection or drag-and-drop yet | done, unreleased | Services/DocumentCollections.cs (new), MainWindow.xaml.cs (collections.* bridge, openPath), ui/start.js, css/app.css; tests/host (collections), tests/e2e/suites/collections.mjs |
| View bar (page, zoom, fit, rotate, layout) floating over the document | done | ui/viewbar.js |
| Reduce motion, reduce transparency (performance) | done | themes.js |
| Accessibility V1 (keyboard and focus): document tabs are one Tab stop, named by their file, switched with arrows / Home / End (a mouse click leaves focus, and the arrows, with the document); in an open menu or keyboard-focused tabs, arrows / Home / End aren't page navigation; a menu closed from the keyboard (Esc, Tab, an item) gives focus back to what opened it, and Tab moves on from there; dialogs and the command palette give focus back when they close, to the document when what had it is gone; a hidden sidebar is inert; outline toggles say Expand / Collapse; Settings → Reduce motion also stops the animations run from script (dom.js reducedMotion). Not: full WCAG conformance, screen-reader testing, the PDF's own content | done, unreleased | ui/focus.js (new), ui/tabs.js, ui/menu.js, ui/dialogs.js, ui/palette.js, ui/sidebar.js, shortcuts.js, dom.js, app.js, css/app.css; tests/e2e/suites/accessibility.mjs |
| Two-page spread layout: pages side by side (1–2, 3–4…, an odd last page alone) in continuous or single-page view, from the view bar or the palette; Previous / Next turn whole spreads; stays on the same page when switched; fitted zooms fit the pair; layout only, the file is never changed. Remembered per file with the page, zoom and single/continuous view (the recent-files entry), so a file reopens in its last layout; new files start without spreads | done, unreleased | spread.js, document-view.js (setSpread), ui/viewbar.js, Services/RecentFiles.cs; tests/editing/spread.test.mjs, tests/host (recent files), tests/e2e/suites/spread.mjs |
| Full-screen reading | planned | — |

## Roadmap — must not be faked

Everything not built yet. Order within a status is not a schedule. The Vision section names the
intended behaviour and its rules.

| Feature | Status | Vision |
|---|---|---|
| Font selection: multiple selectable document fonts (the document's own, standard, bundled), compatibility checks (glyphs, embedding permission, PDF/A), embedding when used, no silent substitution. Done (see Editing): new text in any of the standard families, and in bundled fonts through the font set with the vendored font parser (fontkit), subset-embedded when saved. Done too: new text in the document's own fonts, in glyphs the file already draws. Done too: bold and italic of text the file already draws, in the faces of its font the same file has. What remains: text the file already draws in another font family, characters a document font has but the document never draws, PDF/A with embedded fonts, and scripts that need shaping | planned | §4.3 |
| Bundled selectable font: Liu San — one optional document-editing font available alongside other selectable fonts; a normal entry in the font selector, nothing more. Done: the four faces the owner supplied (regular, bold, italic, bold italic; a modified version of Comic Neue under the SIL OFL 1.1, with OFL.txt and ATTRIBUTION.txt beside them) in web/fonts/document/liusan, one entry in editing/objects/bundled-fonts.js (group sans) and in NOTICE.txt. The supplied files said "preview & print" embedding (OS/2 fsType 4) and had no weight or style bits; Vellum sets fsType to installable (the OFL allows embedding), the bold faces' weight class to 700 and the fsSelection and macStyle bits to each face, leaving outlines, metrics and character map unchanged; its names (family, full, unique and PostScript names in the name table, files and folder) are Liu San / LiuSan-<style> since the owner renamed it (2026-09-16). The font menu shows its name in its own bold face (bundled-fonts.js `preview`), its regular reading too light beside its neighbours; text is written in the face chosen. The font gives no underline metrics, so a bundled face without them is underlined as a document font is (a tenth of an em below, a twentieth thick). In the font menu and the palette like any bundled family; tested in use (sentences with punctuation, wrapped lines, every face, missing characters refused, save, reopen, retype; in the app: chosen from the menu, bold, larger, underlined, turned, duplicated, saved and reopened as editable text in an embedded subset) | done, unreleased | web/fonts/document/liusan, editing/objects/bundled-fonts.js (preview), editing/objects/font-set.js (fontkitFace underline, preview), ui/text-editor.js (#fontMenu), ui/menu.js (weight, italic); tests/editing/bundled-fonts.test.mjs, tests/e2e/suites/manipulation.mjs | §4.4 |
| Writing scripts the document's fonts and the standard fonts don't have (e.g. Devanagari, CJK) | planned | §3.2 |
| Form XObject editing, phases 1–2: text drawn by a Form XObject is cross-checked against pdf.js glyph for glyph, at its own nesting depth, and recorded per *occurrence* (a form drawn twice is two): XObject key, occurrence index, depth, parent and depth-1 root, its own resources or the drawer’s, matrix, BBox, transparency group, the health of its own content stream, and the blockers and notes against rewriting it. Phase 2 makes ONE case editable — a run drawn directly by a depth-1 occurrence that has /Resources of its own, whose glyphs came through the cross-check clean, that nothing else refuses, whose form holds no layer, soft mask, tagged content (an MCID at the `Do` or anywhere inside) or structural trouble, and that names nothing the form’s own resources don’t hold. Such an edit is written into a PRIVATE COPY of that form: the form is copied with its dictionary and a clone of its resources, the glyphs are neutralised in the copy’s own bytes and the new text drawn after the copy’s own content — so it keeps the form’s clip, group and drawing order — and only that occurrence’s `Do` is repointed. The shared original is never changed and no other occurrence moves. Retyping and deleting only: moving, scaling, turning and pasting a copy stay refused for "form" (the copy has nowhere else to be), as do reflow and every occurrence the list above excludes. Deliberately not done: "edit all instances", nested (depth > 1) form editing, carrying page-level resources into a copy, and any UI of its own — such text is simply editable text. Longer replacement text is clipped by the form’s BBox, as the original was. In editing/content/interpreter.js (finishForms, FORM_BLOCKERS, FORM_NOTES, shownResources), editing/runs.js (verifyPage, verifyInForms, formEditOf, explainForm), editing/source.js, editing/content/writer.js (spliceContent), editing/objects/form-copy.js, editing/objects/text-run.js and editing/objects/capabilities.js; tests/editing/form-xobjects.test.mjs, tests/editing/form-xobject-edits.test.mjs | done, unreleased | §3.2 |
| Image crop; inline image replacement; replacing a picture in a PDF/A document (needs a check of the image against the file's output intent); vector-shape editing | planned | §3.2 |
| Shapes, lines, arrows, freehand as page content; stamps and reusable custom stamps | planned | §3.2 |
| Tag-preserving edits; PDF/A font embedding; keeping edited text on its layer | planned | §2.2, §10 |
| Annotations: strikethrough, shapes, stamps, text boxes, measure (also listed under Annotating); moving annotations and links with content | planned | §3.3 |
| Full-screen reading (also listed under App; two-page spreads are done) | planned | §3.1 |
| Fill & sign: signatures (type / draw / upload, place, resize) | done, unreleased — Fill & Sign v1 (Forms) | §3.7 |
| Form creation, form flattening as an explicit user-requested output (detecting and filling existing fields: Forms) | planned | §3.7 |
| Digital (certificate) signatures, separate from signature pictures: signing with a certificate, validity display (needs a local signing/certificate engine chosen) | planned | §3.7 |
| True redaction, beyond v1: vector content in an area removed (v1 refuses the redaction instead); the same information where it is repeated (metadata, bookmarks, annotations, form values, tagged-structure text) | research — open decision §13.2 | §3.6 |
| Password protection: add / change / remove (with the correct password); permissions (print, copy, edit) | planned | §3.6 |
| Compress; metadata editing; metadata privacy cleaning; text/image extraction | planned | §3.4 |
| Crop pages, page numbers, watermarks (0.8): Crop pages…, Page numbers… and Watermark… in the page thumbnails menus and the command palette, for the selected pages or all pages. Each is a setting on the page plan entry (crop margins, number format/position/size/start, watermark text/position/size/opacity/rotation), so it follows moves, duplicates and copies, is one undo step, and can be removed. Crop is the page's /CropBox — nothing is deleted or rasterized, the hidden parts stay in the file. Numbers ({n}, {total}) and watermarks are real text in Helvetica (a standard PDF font), upright as the page is shown and inside /Artifact marked content; once saved they are ordinary page content. Limits: one number and one watermark per page; text only in the WinAnsi character set (other characters are refused in the dialog) | done, unreleased | pages/stamps.js, pages/plan.js (setPageSetting), pages/actions.js (crop, pageNumbers, watermark), document-view.js (setPageSetting), annotations/persist.js, commands.js; tests/editing/page-stamps.test.mjs, tests/e2e/suites/page-stamps.mjs |
| Picture watermarks: Watermark… has Text / Picture. A PNG or JPEG (chosen through the picture dialog, checked by readPicture: other or damaged files are refused in the dialog) goes over the selected pages or all pages, with position, width (% of the shown page), opacity and rotation, and a thumbnail of the picture in the dialog. It is the same page-plan setting as a text watermark (`watermark.picture`, its bytes in the document's sources), so it follows moves, duplicates and copies, is one undo step and can be removed. Written as a real image XObject drawn inside the same /Artifact content, embedded once however many pages carry it; a PNG keeps its transparency (its soft mask), a JPEG goes in byte for byte; the page is never rasterized. Limits: one watermark per page (text or picture); three positions; no tiling | done, unreleased | pages/stamps.js, pages/actions.js (watermark, picturePicker), editing/objects/image.js (readPicture, embedPictures); tests/editing/page-stamps.test.mjs, tests/e2e/suites/page-stamps.mjs |
| Merge several files at once; mixed-format merge | planned / research (mixed formats need §3.5 engines) | §3.4 |
| PDF diagnostics, repair, PDF/A validation | research | §3.4 |
| PDF → JPG / PNG | planned | §3.5 |
| PDF ↔ Word / Excel / PowerPoint; PDF → PDF/A (needs a local conversion engine) | research | §3.5 |
| OCR v1: invisible searchable text layer on scanned pages (current page or whole document; pages with text skipped), progress, per-page failures reported, English only; Tesseract 5 as WebAssembly (tesseract.js 7) with the English model in `web/vendor/tesseract`, run locally (`js/ocr/`, `editing/objects/ocr-text.js`; e2e suite `ocr`) | done, unreleased | §3.8 |
| OCR language packs: Settings → OCR lists English (built in) as Installed and twelve packs (Latin script: Danish, Dutch, Finnish, French, German, Italian, Norwegian, Portuguese, Spanish, Swedish; Cyrillic: Russian; Devanagari: Hindi) as Available; Download (only on click, with progress and Cancel), Use, Remove. A pack is the tesseract.js 4.0.0_best_int model (the bundled English one's source), pinned to one naptha/tessdata commit on GitHub, stored in %LOCALAPPDATA%\Vellum\ocr-languages, never in the install; it becomes usable only once its size and SHA-256 match the list, and is checked again each time OCR loads it (a changed file is removed). OCR uses the chosen language; a chosen pack that is missing or damaged is explained before OCR starts (Use English / Open OCR settings). Latin-script packs are written in the OCR text layer in Helvetica (WinAnsi); Russian and Hindi in an embedded Unicode font (OCR Unicode V1, next row) | done, unreleased | ocr/languages.json, ocr/languages.js, ocr/language-packs.js, ocr/engine.js, ocr/actions.js, ui/settings.js (ocr), Services/OcrLanguages.cs, MainWindow.Ocr.cs, Hosting/AppResourceServer.cs (/ocr-lang/); tests/editing/ocr-languages.test.mjs, tests/host, tests/e2e/suites/ocr-languages.mjs |
| OCR Unicode V1 (Cyrillic, Devanagari): Russian and Hindi (the pinned 4.0.0_best_int `rus` and `hin` packs, downloaded and verified like the others) are read by the same Tesseract engine and written as the same invisible text layer, but in the bundled Noto Sans (OFL), which has the Cyrillic and Devanagari glyphs, embedded as a subset of the glyphs its words use with a ToUnicode map, so the text can be found, selected and copied in Vellum and other readers and survives saving and reopening. The words are laid out one glyph per character, never shaped: the layer is invisible, and Hindi's vowel signs and conjuncts stay in reading order. The font is chosen by the record's language (`UNICODE_FONTS` in ocr-text.js: `rus`, `hin`); every other language is written in Helvetica exactly as before. A character Noto Sans has no glyph for is left out of the layer, as Helvetica leaves out what WinAnsi can't write. Not: other non-Latin scripts, per-word script detection, Devanagari in visible text (still refused unshaped) | done, unreleased | editing/objects/ocr-text.js (UNICODE_FONTS, prepare, unshaped), ocr/languages.json (rus, hin); tests/editing/ocr-text.test.mjs, tests/editing/ocr-languages.test.mjs, tests/host, tests/e2e/suites/ocr-unicode.mjs | §3.8 |
| OCR: other non-Latin scripts (Greek, other Cyrillic and Devanagari languages, other Indic scripts, CJK, Arabic…): a left-to-right script whose letters Noto Sans has (Greek, further Cyrillic or Devanagari languages) needs a pack and a UNICODE_FONTS entry; right-to-left scripts (Arabic, Hebrew) or a font Vellum doesn't bundle (other Indic scripts, CJK) need more | planned | §3.8 |
| Compare documents (PDF Compare V1): "Compare documents…" in the More menu and the command palette. Document A starts as the active tab and B as another open tab; either can be chosen from a file. A read-only window over the documents: pages side by side in one scroll (so both sides move together), matched by their words — pages in place, moved (reordered), removed (only in A) and added (only in B); within matched pages, text added, removed and changed (a word diff), marked on both pages. The change list and total count; F7 / Shift+F7 and buttons for next / previous change; clicking a change (or its mark) scrolls to its place. Overlay (a pixel comparison: ink only in A red, only in B green, the rest faded) and Blink (A and B in turn). Both files are only read, never changed. Pages are read one at a time (text only), then matched and diffed with pauses; a page is drawn only when it scrolls near the view, and the Overlay comparison only for those pages. Not: pictures, drawings or formatting in the change list (Overlay shows them), text on pages without text (scans, unless OCR'd), accepting or rejecting changes, AI summaries, saving a report | done, unreleased | compare/diff.js, compare/compare-view.js, compare/actions.js, commands.js (tools.compare), shortcuts.js (data-own-keys); tests/editing/compare.test.mjs, tests/e2e/suites/compare.mjs | §3.9 |
| Compare V1.1, one type of change at a time: above the change list, All / Added / Removed / Changed / Moved, each with its count (text and page changes of a type together; a type with none can’t be chosen). The list and the marks on the pages show only that type; F7 / Shift+F7 and the next / previous buttons step through only those changes, wrapping; the position counts among them (“– / N” while the selected change is of another type). All is V1 as it was. A view over the change list only: nothing is compared again, and both files are still only read. Page outlines and row badges stay. Not: several types at once, filtering by page or by text, remembering the choice | done, unreleased | compare/filter.js, compare/compare-view.js, css/app.css; tests/editing/compare.test.mjs, tests/e2e/suites/compare.mjs | owner request, 2026-09-22 |
| Document history V1: "Document history…" in the More menu and the command palette, for a document open from disk. Manual snapshots only, with an optional name: each is an exact copy of the file as saved, kept on this PC in %LOCALAPPDATA%Vellumhistory (one folder per document path, an index.json and one PDF per snapshot); never uploaded, synced or shared. The list shows each snapshot’s name, when it was taken and its size on disk, newest first, with the history’s total storage below it (updated at once on delete or clear); a document with unsaved changes asks to save first or snapshot the saved version. Open: the snapshot in a tab of its own, read-only (the host refuses writes to it; Save becomes Save As; not a recent file). Compare: PDF Compare with the snapshot as A and the document as saved as B. Restore (asks first): the current saved version is kept as a new snapshot (“Before restoring …”), then the snapshot’s bytes are written into the document through the normal atomic save and the tab reloads; the snapshot restored is never changed. Delete (asks first) removes one snapshot’s file. Clear history (asks first, with the count and size) removes every snapshot of the document: its history folder only, never the document. Not: automatic or timed versions, snapshots of unsaved changes, history following a file that is renamed or moved outside Vellum, a size limit or automatic clean-up of old snapshots, cloud or shared history | done, unreleased | Services/DocumentHistory.cs, Hosting/AppResourceServer.cs (RegisterReadOnlyDocument, IsWritable), MainWindow.xaml.cs (history.*), history/model.js, history/actions.js, app.js (readOnly files), commands.js (file.history), css/app.css; tests/editing/history.test.mjs, tests/e2e/suites/history.mjs | owner request, 2026-09-17 (not yet in VELLUM_VISION.md) |
| History storage across documents: Settings → History lists every document with snapshots on this PC (read from Vellum’s history folder only), most recent first: name, original path, snapshot count, size on disk and last snapshot, with the total above. Open history opens the document (it must still be at its path) and its own history dialog. Clear (asks first) removes that document’s history folder only. A document no longer at its path (or whose index can’t be read) is marked missing: its history can’t be opened, only removed, one at a time or with “Remove missing” (asks first; the host re-checks that each is still missing). Requests name a history by its folder key, never a path, and only a folder directly inside the history folder is ever deleted; never a PDF. Not: automatic clean-up, size limits, cloud sync, following files renamed or moved outside Vellum | done, unreleased | Services/DocumentHistory.cs (Stored, DocumentFor, RemoveStored, RemoveMissing), MainWindow.xaml.cs (history.stored, history.openStored, history.removeStored, history.removeMissing), history/model.js, history/actions.js, ui/settings.js, app.js (settings context), css/app.css; tests/editing/history.test.mjs, tests/e2e/suites/history-storage.mjs | owner request, 2026-09-22 (not yet in VELLUM_VISION.md) |
| History follows Save As: when a document is saved under a new name or in another folder with Save As, its history moves with it (the same snapshot files, names, times and sizes); the old path keeps none, so Settings never lists it as missing, and Restore and Compare work from the new file. Only the document’s own history folder moves (its index must name the old path). When the new path already has history of its own, nothing moves and nothing is merged: both histories stay as they were and Vellum says so. PDFs are never touched. Not: following files renamed or moved outside Vellum, merging two histories, Extract pages (a new document, not a move) | done, unreleased | Services/DocumentHistory.cs (Move), MainWindow.xaml.cs (history.move), app.js (saveView); tests/host/Program.cs, tests/e2e/suites/history-move.mjs | owner request, 2026-09-22 (not yet in VELLUM_VISION.md) |
| Semantic document model V1 (internal, no UI): one read-only description of a document for later features to read — pages (box, rotation), text blocks (paragraphs from paragraph grouping, every other line a block of its own), text runs (font, size, direction, editability), images, form fields, annotations and links, each with a box in PDF user space and an in-memory ID (`p<page>:<key>`) that is the same on every build; reading order only as far as the analysis vouches for it (lines of a paragraph top down, blocks in content order). An adapter over the object model, paragraph grouping and pdf.js annotations; no analysis of its own. Not: columns or reading order inferred from layout, paths and form XObjects, Vellum’s own annotations and unsaved edits’ positions, tables, headings, search, AI | done, unreleased | semantic/model.js; tests/editing/semantic-model.test.mjs | — |
| Document structure inspector V1: “Document structure” in the More menu and the command palette opens a Structure tab in the sidebar (shown once opened, for the session; the three tabs then show icons only). The semantic document model of the active document, read-only: the page count, then each page — read when it is opened (the page shown opens first) — with its object count and groups for text (paragraphs with their runs; a one-line block is its run), images, form fields, annotations and links, each with its count. Selecting an object goes to its page, outlines its box on the page for a moment (scrolled into view) and shows its properties: text run text, font, size, editable, invisible; paragraph lines and text; image pixels, type (image object, inline, added in Vellum); field name, type, value, read-only; annotation subtype and contents; link destination (URL, the page an internal link goes to when pdf.js resolves it, else a place in the document); size and position on the page and the model ID for all. Nothing in the document, the model or Edit mode’s selection changes. V1.1: a protected (encrypted) PDF, opened with its password, is read from pdf.js alone — page size and rotation, form fields, annotations and links, with a note that text and images aren’t read (the editing session doesn’t analyse protected PDFs); a content or page change made in Vellum while the tab is open reads again the pages it touched (edited text, moved objects) or, after a page change, every open page. Not: editing from the panel, reading every page up front, text or images of a protected PDF, annotations and form values added in Vellum but not yet saved (the model reads the file’s), search, headings, AI | done, unreleased | ui/structure.js, semantic/inspector.js, semantic/model.js (readSessionPage, readPdfPage, linkTargets), ui/sidebar.js, commands.js (tools.structure), app.js, css/app.css; tests/editing/semantic-inspector.test.mjs, tests/e2e/suites/structure.mjs | owner request, 2026-09-17 (not yet in VELLUM_VISION.md) |
| Semantic search V1: a search field at the top of the Structure tab, over the semantic document model — deterministic, local string matching, not AI and not the viewer’s Find (which stays as it is). A query is words (text, form field names and values, note contents and link URLs containing them), a kind (`all images`, `all form fields`, `all annotations`, `all links`, `all text`), an editable filter (`editable text containing method`, `non-editable text`: per text run) and `containing` or `exactly` (the whole text, white space and case ignored); quotes search the words themselves. Results list kind, label and page, in page order (text in reading order, then images, fields, annotations, links), in place of the tree while a query is typed; Enter / Shift+Enter and Previous / Next select them in turn (wrapping), which goes to the page, outlines the box and shows the properties as selecting in the tree does; Escape clears it. Pages are read one after another as the search reaches them (kept for the tree), yielding between pages; a new query stops the old one; a query only for fields, annotations or links reads pdf.js’s annotations, not the page’s content; at most 1,000 results; a content change re-runs the search. Not: embeddings, an index, OCR, fuzzy or stemmed matching, regular expressions, text or images of a protected PDF, matching images by anything but kind | done, unreleased | semantic/query.js, ui/structure.js, css/app.css; tests/editing/semantic-search.test.mjs, tests/e2e/suites/semantic-search.mjs | owner request, 2026-09-17 (not yet in VELLUM_VISION.md) |
| PDF health V1: “PDF health…” in the More menu and the command palette checks the active document, read-only, and lists what Vellum can prove about it before editing, each finding with a severity (won’t be changed, limited, good to know), a category, its pages (a page number goes to that page) and, for text, how many pieces of text it applies to. Every finding is a signal Vellum already produces, in the words it already uses; nothing is parsed again: protected (encrypted) — pdf.js’s EncryptFilterName, with the security handler — and its pages then not read; an XFA form (pdf.js’s IsXFAPresent: Vellum reads only the AcroForm fields); digitally signed or certified, tagged for accessibility and a PDF/A claim (the document profile); per page, from the editing session’s own verified analysis: unreadable content, unbalanced drawing instructions, a page the engine can’t read at all, scanned pages with or without a text layer, and text the engine won’t edit, by its refusal reason (REASONS), with the blockers of each Form XObject that holds text (FORM_BLOCKERS). Pages are read one after another with progress; closing the dialog stops the check. Not: a score, guessed corruption, repair, validation against a standard (PDF/A, PDF/UA), network or AI analysis | done, unreleased | health/health.js (documentFindings, pageFindings, mergeFindings, checkDocument), health/actions.js, commands.js (tools.health), app.js, css/app.css; tests/editing/health.test.mjs, tests/e2e/suites/health.mjs | owner request, 2026-09-22 (not yet in VELLUM_VISION.md) |
| Semantic search V1.1: Match case and Whole words buttons beside the Structure search field (the Find bar’s icons and meaning, via editing/find-replace.js findMatches; `exactly` honours Match case); a kind word alone or before other words (`text`, `images`, `links`, `fields`, `comments`…) is searched for as a word — it is a kind only after `all` or an editable filter, or before `containing` / `exactly`; the status line counts results as pages are read (“3 found · reading page 4 of 9…”), then “N results” or “i of N”. Query text keeps its case. The Find bar and its own options are unchanged. Not: regular expressions, fuzzy or stemmed matching, remembering the options between sessions | done, unreleased | semantic/query.js, ui/structure.js; tests/editing/semantic-search.test.mjs, tests/e2e/suites/semantic-search.mjs | owner request, 2026-09-22 |
| Table extraction V1: “Copy tables on this page” in the command palette copies the text tables on the current page as tab-separated text (one line per row, tables a blank line apart), to paste into a spreadsheet; otherwise it says no table was confidently detected. Read-only and deterministic, over the semantic document model only (semantic/tables.js pageTables: rows, columns and cells with their text, box, page and run IDs; empty cells null) — no AI, no OCR, no rendering, nothing written. A table is reported only on strong evidence: at least 3 evenly spaced lines of at least 2 cells (runs an em or more apart; a gap between a word space and an em makes the line ambiguous), no paragraph line among them, at least 2 columns with no line putting two cells in one, each column filled on half the rows or more and lined up (left, right or centre), the table three quarters full, and short cells rather than running text. Anything table-like short of that is reported as not confidently detected, with the reason. Not handled: merged or spanning cells, nested tables, rotated text, tables that are pictures, ruling lines; protected PDFs (their text isn’t read) | done, unreleased | semantic/tables.js, app.js (copyPageTables), commands.js; tests/editing/table-extraction.test.mjs | owner request, 2026-09-22 |
| Research mode V1: “Research this document” in the command palette, or the Research button beside the Structure search field, takes the field for a question about the document. Its key terms (stop words dropped, quoted phrases kept whole) are each searched for as text with semantic search (semantic/query.js matchPage; terms of three letters or fewer as whole words), every page read as the search reads them; a passage (a paragraph, or a line of its own) is evidence when it holds all of one or two terms, else half or more. At most 8 passages, most terms first, then in page and reading order, each quoted from the document with its page, kind and the terms it holds, under one summary line marked as Vellum’s (how many passages, on which pages, terms found nowhere), never an answer. No passage holding enough terms: “Not enough evidence in this document”, with the terms found nowhere; a protected PDF says its text isn’t read. Selecting a passage goes to its page, marks its box and shows its properties, as a search result does; Enter / Previous / Next step through them. Read-only, local and deterministic; the panel exposes the result (`structure.research`) for later features. Not: AI or generated answers, embeddings or an index, a second search engine, stemming or synonyms, several documents, OCR, protected PDFs’ text | done, unreleased | semantic/research.js, ui/structure.js, ui/sidebar.js (showResearch), commands.js (tools.research), css/app.css; tests/editing/research.test.mjs, tests/e2e/suites/research.mjs | owner request, 2026-09-22 (not yet in VELLUM_VISION.md) |
| Collection research V1: “Research” beside a collection on the home screen asks one question of the documents in that collection. The question’s key terms, the passage rule and the ranking are Research mode V1’s (semantic/research.js over semantic search), over one collection only — no second search engine, no index, no embeddings, no AI. Each document is read once, one at a time, from a read-only URL the host gives for it (collections.documents, AppResourceServer.RegisterReadOnlyDocument): its pages are analyzed by the editor’s own page analysis into the semantic model, the passages holding a term are kept and the document is dropped again — none is opened in Vellum, none is written. One ranked list over the whole collection, at most 8 passages (the same limit one document has), most terms first, then in the collection’s order and page order; each quoted with its file name, its page and the terms it holds, under one summary line marked as Vellum’s. Choosing a passage opens that document (openPath now also accepts a path a collection lists) at its page with its box outlined (ui/page-mark.js, shared with the Structure panel). A file that is gone, protected or unreadable is skipped, counted and named with its reason; a question with no key terms, or one no passage answers, says so instead of guessing. Single-document Research is unchanged. Not: cross-collection research, background indexing, a saved or resumable result, stepping through results with Enter, passwords asked for another document, protected documents’ text, AI or generated answers | done, unreleased | semantic/collection-research.js (new), ui/collection-research.js (new), ui/page-mark.js (new, from ui/structure.js), semantic/research.js (document order in the ranking), ui/start.js, app.js, MainWindow.xaml.cs (collections.documents, openPath), css/app.css; tests/editing/collection-research.test.mjs, tests/e2e/suites/collection-research.mjs | owner request, 2026-09-23 (not yet in VELLUM_VISION.md) |
| Research evidence export V1: “Export evidence” beside the Research field (single-document Research) and beside a collection’s research dialog (Collection Research) copies the evidence shown now as tab-separated text (semantic/research.js evidenceToTsv) — one row per passage, in the shown (ranked) order: the research question, the document it came from (the open document’s file name for single-document Research; the passage’s own document for Collection Research), its page, the quoted evidence, the terms matched and its kind — with a header row, ready to paste into a spreadsheet. Uses the existing clipboard pattern (commands.js copyText, as Table extraction V1 does), so no file-save dialog or new C# bridge. No evidence to export (no question asked, or not enough evidence found): the button is disabled or says so, nothing is copied. Not: PDF modification, cloud or network, AI, a new indexing or search system, a file-save dialog | done, unreleased | semantic/research.js (evidenceToTsv), ui/structure.js (export button beside Research), ui/collection-research.js (export button); tests/editing/research.test.mjs | owner request, 2026-09-23 (not yet in VELLUM_VISION.md) |
| Document provenance V1 (internal, almost no UI): every Research and Collection research evidence passage carries a provenance record (semantic/provenance.js) of where it came from — { v, source, document: { name, path, contentKey, reference }, page, box, object: { id, kind, blockId, runIds } } — built only from what Vellum already holds: the document reference, the 1-based page, the box the semantic model gives and that model’s own IDs for the passage. `source` is which research found it (document-research or collection-research). `contentKey` is the SHA-256 the host already returns with a document’s bytes (X-Vellum-Doc-Key, AppResourceServer, the key protected documents’ annotations are kept under); the open document takes it from the view (docKey), a collection document from the read-only URL it is read over, and a reader that gives none leaves it null and `reference` says the file is referenced by its path. No new database, index, identity store, ID scheme or embedding: the record is frozen, JSON-safe and exposed as it stands through the result models Research already has (structure.research, now with source and document, and researchCollection’s evidence), so a later feature can quote or reach a source without reading the PDF again. Visible in the existing output as the evidence row’s tooltip (file, page, object ID, path, content key). Not: identity that survives editing or Save As (the content key is the bytes as they were read, and Vellum stores no document identity that outlives them), a second document index, object identity guessed from geometry, AI or embedding IDs, a new export format or command | done, unreleased | semantic/provenance.js (new), semantic/research.js (IDs kept on a candidate; provenance and source on ranked evidence), semantic/collection-research.js (the host’s content key, per-document reference), ui/structure.js (document reference, row tooltip, research getter), ui/collection-research.js (row tooltip); tests/editing/provenance.test.mjs, tests/e2e/suites/research.mjs, tests/e2e/suites/collection-research.mjs | owner request, 2026-09-23 (not yet in VELLUM_VISION.md) |
| Knowledge / document graph V1: “Graph” beside a collection on the home screen, and “Document graph…” in the More menu and the command palette for the open document, list the relationships Vellum can already prove about that one — read-only, derived on demand and thrown away again. Four kinds of node (collection, document, page, research evidence) and four kinds of edge (document MEMBER_OF collection, document CONTAINS page, evidence FROM page or document, evidence HAS_PROVENANCE document, carrying the provenance record’s reference, content key and semantic model IDs). Everything comes from systems that already own it: Document Collections (the host’s collections.list, the collection’s own order), the semantic document model and Document provenance V1 through Research and Collection research evidence — the collection graph shows the evidence of the last research asked of that collection in this session, the document graph the evidence the Structure panel’s research is showing, and nothing is re-read, re-searched or kept. A document is matched by the path the collection already holds (case-folded as the host matches it): a matching key, not a new identity, and no second document index. Page nodes exist only for pages the graph has something to show for. A document that is gone stays a node, listed as not found and not openable, and the view says how many. Choosing a document, page or evidence row opens that source through the existing actions (openPath, and evidence at its page with its box outlined). Not: a stored or persistent graph, a graph database, background indexing, an interactive node-and-edge visualization, cross-collection or whole-library graphs, similarity or “related document” inference, entity or topic extraction, embeddings, AI, or any relationship Vellum cannot prove from what it already holds | done, unreleased | semantic/knowledge-graph.js (new), ui/knowledge-graph.js (new), ui/collection-research.js (onResult: the last research reported to its caller), ui/start.js, app.js (showDocumentGraph, More menu), commands.js (tools.graph), css/app.css; tests/editing/knowledge-graph.test.mjs, tests/e2e/suites/document-graph.mjs | owner request, 2026-09-23 (not yet in VELLUM_VISION.md) |
| Saved research V1: “Save this research” beside the Research field (single-document Research) and in a collection’s research dialog (Collection research) keeps the result shown now on this PC, under a name the person gives — the question, which research answered it, the document or the collection it was asked of, the ranked evidence as it was found (quote, page, box, matched terms, the model’s IDs) and each passage’s provenance record (Document provenance V1), with the time it was saved. The home screen lists them under “Saved research”, in the same row pattern the collections use; Open shows the result back read-only — the same evidence and the same summary Vellum wrote then, never rewritten — and choosing a passage opens its document at its page with its box outlined, through the existing actions (openPath now also accepts a path a saved result quotes). Opening one never asks the question again, never reads a PDF and never refreshes or recomputes anything. Kept in %LOCALAPPDATA%\Vellum\research.json by the same local-persistence pattern the collections and the recent list use (Services/SavedResearch.cs: atomic write, and a damaged file starts over with the old one kept as research.json.bad; a hand-edited item without an id, a name or a result is dropped and the whole ones still open). The host stores the result whole and reads only the name and the document paths it is told, so the shape of a result stays the page’s; a document that has moved or gone stays the one the evidence names, listed as not found and not openable. Delete removes that one saved result and nothing else — no document, no collection, no other item, and no PDF is written. Not: a second document index, embeddings or a vector store, AI, background indexing, a permanent document identity (the content key still means only what Document provenance V1 says), a silent refresh or recompute of a saved result, cross-result search, editing a saved result, exporting or importing them as files, sharing, cloud or network | done, unreleased | semantic/saved-research.js (new), ui/saved-research.js (new), Services/SavedResearch.cs (new), MainWindow.xaml.cs (research.list, research.save, research.delete; openPath), ui/structure.js (Save beside Research; `kind` on the research getter), ui/collection-research.js (Save in the dialog), ui/start.js (the Saved research section), css/app.css; tests/editing/saved-research.test.mjs, tests/host/Program.cs, tests/e2e/suites/saved-research.mjs | owner request, 2026-09-23 (not yet in VELLUM_VISION.md) |
| Export Center V1: “Export…” in the More menu and the command palette (Ctrl+Shift+E) writes new files from the open document: JPEG images, PNG images, or one Markdown file. One dialog picks the format, the pages (all, this page, or a range in the same syntax the page dialogs use) and the folder, which starts as the document’s own; progress shows each file and Cancel stops before the next one, so a file is either written whole or not at all. File names are deterministic — “report (page 003).jpg”, zero-padded to the document’s page count, “report.md” for Markdown — and a name already taken asks once: Replace, Keep both (numbered) or Cancel. The result says what was written, what could not be and why, with Show to open the folder. The source PDF is never opened for writing and is byte for byte unchanged. One shared export contract (export/model.js formats, page selection, plan and names; export/run.js the produce → write → progress → result loop) with one exporter per format, so PDF → Word, Excel and PowerPoint can later plug into the same contract without a second pages, naming, overwrite, progress or cancellation system. Images are rendered by the pdf.js renderer printing already uses (export/images.js, 150 dpi, on white, with the annotations Vellum drew painted on top) — no second renderer and no image library. Markdown is written only from what Vellum already reads (export/markdown.js): the semantic document model’s paragraphs and lines in its reading order, the tables Table extraction V1 is confident about as GitHub tables (their first row is the header row a GitHub table needs; no header is inferred), a heading only where the PDF’s own outline names that exact text on that page, one “## Page N” per page, and a closing note with the document’s name, path and content key from Document provenance V1 — nothing is guessed from font sizes, and a protected PDF’s unread page says so. The host decides where an export may write (MainWindow.Export.cs): a folder the person chose in this session, or the folder the open document is in; names are cleaned again there and each file gets a one-shot write token (AppResourceServer /export/{token}, the same atomic write /save uses, without the PDF check). Not in V1: Word, Excel, PowerPoint, TIFF or SVG, a resolution or quality setting, exporting several documents at once, a plugin framework, cloud, network or AI | done, unreleased | export/model.js, export/run.js, export/images.js, export/markdown.js, export/actions.js (all new), ui/export.js (new), MainWindow.Export.cs (new), Hosting/AppResourceServer.cs (RegisterExportFile, IsKnownFolder, /export/{token}), MainWindow.xaml.cs (registers the handlers), app.js, commands.js (file.export), css/app.css; tests/editing/export.test.mjs, tests/e2e/suites/export.mjs | owner request, 2026-09-23 (not yet in VELLUM_VISION.md) |
| PDF → Excel V1: “Excel workbook” in the Export dialog (Export Center V1) writes one .xlsx from the pages chosen — every table Table extraction V1 is confident about becomes one worksheet of real Excel cells, never a picture. The tables are the extraction’s own (semantic/tables.js over the semantic document model): its rows and columns, in its order, with an empty cell written as an empty cell so a row keeps its shape. There is no second table parser, no OCR fallback, no merged or spanning cells (the extraction reports none) and nothing inferred — a table-like region the extraction was not confident about is left out and named on a closing “About this export” sheet, with the document’s name, path and content key from Document provenance V1. A cell keeps its text unless reading it as a number changes nothing at all (export/xlsx.js cellNumber: a plain decimal that round-trips, so “007”, “1.50”, “1,200” and “12%” stay text). Worksheet names are deterministic (“Page 4”, or “Page 4 table 1” and “Page 4 table 2” where a page has more), and the same document and pages give the same workbook byte for byte — the archive uses the ZIP epoch, not the time of the export. A document with no confident table writes no file at all and says so. Pages, names, the folder, overwriting, progress, cancellation and the result are Export Center V1’s, unchanged. The .xlsx is a ZIP of OOXML parts written here (export/zip.js) and deflated by pako, which the app already carries: no spreadsheet framework and no new dependency. The source PDF is never opened for writing. Not in V1: cell formatting, fonts, colours, column widths, formulas, merged cells, charts, several documents at once, reading an .xlsx back in, cloud, network or AI | done, unreleased | export/xlsx.js, export/zip.js (both new), export/model.js (the excel format, kind ‘binary’), export/actions.js (the producer, and one shared page read for every format that works from the semantic model); tests/editing/export-excel.test.mjs | owner request, 2026-09-23 (not yet in VELLUM_VISION.md) |
| PDF → Word V1: “Word document” in the Export dialog (Export Center V1) writes one editable .docx from the pages chosen. The source is the semantic document model — the same one the Markdown export writes from — and nothing else: its paragraphs and lines as Word paragraphs, in its reading order; bold and italic only where the PDF’s own font name for that run says so (“Arial-BoldMT”, “Helvetica-Oblique”, and “Boldoni” is neither); the tables Table extraction V1 is confident about as Word tables of real cells, their columns as wide as the model’s own boxes and their empty cells kept; a Word hyperlink where a link annotation’s own rectangle covers a run; a page break between one page and the next; and a heading only where the PDF’s outline names that exact text on that page (Heading 1–4). It closes with the document’s name, path and content key from Document provenance V1. There is no second PDF parser, no second semantic model, no page rasterized and no layout read off the page: nothing is guessed from a font size, no list, column, header or footer is reconstructed, and a page whose text was not read (a protected PDF) says so instead of being filled in. Images are not written — the model holds an image’s place and size but not its pixels, and a picture is never invented or rasterized in its stead — and neither are colours, fonts, sizes, alignment, form fields or annotations. This is a faithful editable document, not a pixel-perfect copy, and it never claims to be one. Deterministic: the same pages always give the same bytes. The .docx is a ZIP of OOXML parts (export/zip.js, shared with PDF → Excel V1) deflated by pako: no document-conversion framework and no new dependency. Pages, names, the folder, overwriting, progress, cancellation and the result are Export Center V1’s, unchanged; with five formats the dialog’s choices now scroll so its folder, note and buttons stay in view. The source PDF is never opened for writing. Not in V1: images, pixel-perfect layout, columns, lists, headers and footers, styles or fonts from the PDF, internal links and bookmarks, comments, form fields, tracked changes, reading a .docx back in, cloud, network or AI | done, unreleased | export/docx.js (new), export/model.js (the word format), export/actions.js (the producer), semantic/tables.js (blockTables: which confident table each block belongs to, now shared by the Markdown and Word exports), export/markdown.js (uses it), ui/export.js, css/app.css (the dialog’s choices scroll); tests/editing/export-word.test.mjs, tests/e2e/suites/export.mjs | owner request, 2026-09-23 (not yet in VELLUM_VISION.md) |
| Vellum Intelligence (AI): local-first, behind `AIProvider` (ARCHITECTURE_GUIDELINES.md). No AI UI until there is a real provider; no cloud provider, API keys or uploads. Summarise, ask, find, explain, translate, extract data, PDF → Markdown, Smart Split, study notes, AI difference summaries | research | §3.9 |
| Vellum Flow: reusable local workflows composing existing operations | planned | §3.10 |
| Batch processing: conversion, OCR, compression, watermarking, metadata cleaning, protection, export | planned | §3.10 |
| Autosave and crash recovery of unsaved edits | planned | §3.11 |
| Starred files (kept on this computer); task history; Batch Center; Workflow Center | planned | §3.10, §3.11 |
| Templates; extensions (need a definition and a security model) | research | §3.11 |

## Out of scope while Vellum is local-first

Cloud processing, document uploads, cloud storage, sync and shared files, online AI APIs, API keys and
cloud sign-in, telemetry, analytics, mandatory internet connections, third-party SaaS. See "Offline and
privacy" in ARCHITECTURE_GUIDELINES.md.

## Do not build

Password cracking or recovery; encryption, permission or signature bypass; HTML-overlay, rasterised or
guessed editing; whole-page reconstruction as an editing shortcut; silent font substitution; modifying
embedded font programs; a covering box presented as redaction; fake features; changes to
WebView2's security setup (SmartScreen, unsupported switches, Windows settings). See Vision §2.2 and §11.
